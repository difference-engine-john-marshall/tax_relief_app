
<%= form_tag '/dashboard/admins/edit', method: :delete do %>
<div class="container-fluid">
  <div class="row" id="new-title">
    <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12" id="question-text">
      <div class="panel panel-default" id="question-text-panel">
        <div class="panel-heading" id="question-text-panel-heading">
          <h3 class="panel-title">Delete Admin</h3>
        </div>
        <%= form_tag '/dashboard/admins/edit' do %>
          <br>
          <%= text_field_tag :email, nil, placeholder: 'Enter email address...' %>
          <br>
          <br>

        <% end %>
      </div>
    </div>
  </div>
<% end %>
This will remove admin permissions for the user and is not reversable.
<% @admins.each do |admin| %>
<p><%= admin.id %>  <%= admin.email %> <%= submit_tag "Delete Admin", class: "button-submit btn-lg", id: "button-submit" %> </p>
<% end %>

